@model IEnumerable<SurveyServer.Models.Survey>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SurveyID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SurveyTitle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PatternLock)
            </th>
            <th>
                Link
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.SurveyID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SurveyTitle)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PatternLock)
                </td>
                <td>
                    http://120.79.148.199/SurveyServer/SurveyAPI/@Html.DisplayFor(modelItem => item.SurveyID)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.SurveyID">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.SurveyID">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.SurveyID">Delete</a>
                </td>
            </tr>
        }

    </tbody>
</table>